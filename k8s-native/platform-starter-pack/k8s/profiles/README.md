# Profiles

local, test, preview, and production have separate sections. They will share the base and change only environment-specific configuration. Profiles are not deployable yet. Application settings already distinguish these environments; local authentication refuses preview/production.
