# Preview profile

Planned: per-branch namespaces, unique routes and data credentials, quotas, and automatic cleanup. Shared controllers are cluster-scoped; controller upgrades require dedicated test clusters. This profile is not deployable and development authentication refuses preview mode.
