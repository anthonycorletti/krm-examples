# Component inspection

Allows API/MCP and workflow service accounts to read status of three named platform resources: the Gateway, Postgres cluster, and Argo controller. No list, mutation, Secret, or cluster-admin permissions. Application authorization runs before any probe.
