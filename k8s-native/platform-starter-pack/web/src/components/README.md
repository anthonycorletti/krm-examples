# Components

Shared presentation components live here. ui/ contains shadcn components; feature screens live in routes/. The application uses React Router data APIs and the generated Hey API Fetch client, with no TanStack dependency.
