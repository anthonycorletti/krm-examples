#!/bin/sh
set -eu
cd "$(dirname "$0")/../server"
uv run --frozen ruff check --config pyproject.toml . ../bin/lib
uv run --frozen ruff format --config pyproject.toml --check . ../bin/lib
../bin/server-typecheck
uv run --frozen pytest
