#!/bin/sh
set -eu
cd "$(dirname "$0")/.."
./bin/certs
cd server
exec uv run --frozen uvicorn app.mcp:create_http_app --factory --host 127.0.0.1 --port 8001 --reload \
  --ssl-keyfile ../.local/tls/localhost.key --ssl-certfile ../.local/tls/localhost.crt
