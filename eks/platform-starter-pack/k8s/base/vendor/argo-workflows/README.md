# Argo Workflows vendor

Unmodified v4.1.2 install manifest and Apache-2.0 license. URLs and SHA-256 hashes are in sources.txt. Restore with bin/vendor-fetch argo-workflows; verify with bin/vendor-check. Review CRDs, RBAC, controller/executor images and release notes together when upgrading. Namespace overrides live in ../../cluster/argo-workflows/.
