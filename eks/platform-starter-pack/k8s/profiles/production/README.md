# Production profile

Planned: EKS reference resources with OIDC, restricted service identities, encrypted Secrets, persistence, TLS, backups, and controlled releases. No production deployment is implemented or authorized by the local bootstrap. The application rejects production startup while development authentication is the only provider.
