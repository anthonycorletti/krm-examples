apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: platform-identity
spec:
  parentRefs:
  - name: platform
  hostnames: [auth.localhost]
  rules:
  - backendRefs:
    - name: keycloak
      port: 8080
