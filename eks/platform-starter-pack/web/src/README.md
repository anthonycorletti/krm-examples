# Web source

main.tsx composes React Router and the local connection screen. session.ts configures authentication for the generated client without persistent token storage. routes/ owns loaders, actions, and components. client/ is generated; style.css contains the console styles.

Run bin/web-format, bin/web-typecheck, and bin/web-build from the example root.
