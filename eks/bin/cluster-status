#!/bin/sh
set -eu

: "${AWS_REGION:?Set AWS_REGION}"

aws eks describe-cluster \
  --region "$AWS_REGION" \
  --name "${1:-krm-examples}" \
  --query 'cluster.{Name:name,Status:status,Version:version,Health:health}'
