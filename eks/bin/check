#!/bin/sh
set -eu
eks_root=$(CDPATH= cd -- "$(dirname "$0")/.." && pwd)
for command in create-cluster delete-cluster cluster-status cluster-kubeconfig check; do
  sh -n "$eks_root/bin/$command"
done
sh "$eks_root/tests/cluster-commands"
